{% comment %}
  Add the following code to your "sections" folder in your Shopify theme.
  Name the file "custom_hero_section.liquid" or something similar.
{% endcomment %}

{%- assign section_id = 'custom-hero-section-' | append: section.id -%}

<div id="{{ section_id }}" class="custom-hero-section">
  <div class="bg-video-wrap">
    <video id="heroVideo" loop muted autoplay>
      <source src="{{ section.settings.video_source }}" type="video/mp4">
      Your browser does not support the video tag.
    </video>
    <div class="overlay"></div>
    <div class="hero-content">
      <div class="hero-header">
        <input type="text" placeholder="{{ section.settings.header_input_placeholder }}" class="header-input">
        <div class="play-button" onclick="openVideoModal()">
          <img src="path-to-your-play-button-image.png" alt="Play Button">
        </div>
        <div class="cta-buttons">
          <a href="{{ section.settings.cta_button_1_href }}" class="cta-button">{{ section.settings.cta_button_1_text }}</a>
          <a href="{{ section.settings.cta_button_2_href }}" class="cta-button">{{ section.settings.cta_button_2_text }}</a>
        </div>
      </div>
    </div>
  </div>

  <div id="video-modal" class="video-modal">
    <span class="close-modal" onclick="closeVideoModal()">X</span>
    <video class="modal-video" controls>
      <source src="{{ section.settings.video_source }}" type="video/mp4">
      Your browser does not support the video tag.
    </video>
  </div>
</div>

<style>
  /* Add your custom styling here */

  #{{ section_id }} {
    position: relative;
    overflow: hidden;
  }

  .bg-video-wrap {
    position: relative;
    overflow: hidden;
    width: 100%;
    height: 100vh;
  }

  video#heroVideo, .modal-video {
    min-width: 100%;
    min-height: 100vh;
    z-index: 1;
    object-fit: cover;
  }

  .overlay {
    width: 100%;
    height: 100vh;
    position: absolute;
    top: 0;
    left: 0;
    background-image: linear-gradient(45deg, rgba(0,0,0,.3) 50%, rgba(0,0,0,.7) 50%);
    background-size: 3px 3px;
    z-index: 2;
  }

  .hero-content {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    z-index: 3;
  }

  .hero-header {
    text-align: center;
    color: #fff; /* Change as needed */
  }

  .header-input {
    margin-bottom: 10px;
  }

  .play-button {
    cursor: pointer;
  }

  .cta-buttons {
    display: flex;
    justify-content: space-between;
  }

  /* Add more styling as needed */

  /* Video Modal Styles */
  .video-modal {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.8);
    z-index: 4;
  }

  .modal-video {
    width: 80%;
    height: 80%;
    margin: 10% auto;
    display: block;
  }

  .close-modal {
    position: absolute;
    top: 20px;
    right: 20px;
    color: #fff;
    font-size: 20px;
    cursor: pointer;
  }
</style>

<script>
  function openVideoModal() {
    var modal = document.getElementById('video-modal');
    modal.style.display = 'block';
    document.body.style.overflow = 'hidden';
  }

  function closeVideoModal() {
    var modal = document.getElementById('video-modal');
    modal.style.display = 'none';
    document.body.style.overflow = 'auto';
  }
</script>






{% comment %}
  Add the following code at the top of your "custom_hero_section.liquid" file.
{% endcomment %}

{% schema %}
{
  "name": "Video Hero Section",
  "settings": [
    {
      "type": "text",
      "label": "Video Source (YouTube URL)",
      "id": "video_source",
      "default": "https://www.youtube.com/embed/n61ULEU7CO0?autoplay=1&mute=1&controls=0&loop=1",
      "info": "Enter the YouTube video URL for the background. Make sure it's set to autoplay, muted, and looped without controls."
    },
    {
      "type": "text",
      "label": "CTA Button 1 Text",
      "id": "cta_button_1_text",
      "default": "Learn More",
      "info": "Enter the text for CTA button 1."
    },
    {
      "type": "url",
      "label": "CTA Button 1 Href",
      "id": "cta_button_1_href",
      "default": "/",
      "info": "Enter the URL for CTA button 1."
    },
    {
      "type": "text",
      "label": "CTA Button 2 Text",
      "id": "cta_button_2_text",
      "default": "Shop Now",
      "info": "Enter the text for CTA button 2."
    },
    {
      "type": "url",
      "label": "CTA Button 2 Href",
      "id": "cta_button_2_href",
      "default": "/",
      "info": "Enter the URL for CTA button 2."
    },
    {
      "type": "text",
      "label": "Header Input Placeholder",
      "id": "header_input_placeholder",
      "default": "Header Text",
      "info": "Enter the placeholder text for the header input."
    }
  ],
  "presets": [
    {
      "name": "Video Hero Section",
      "category": "Custom"
    }
  ],
  "blocks": [
    {
      "type": "text",
      "name": "Header Input",
      "settings": [
        {
          "type": "text",
          "label": "Placeholder",
          "id": "header_input_placeholder",
          "default": "Header Text"
        }
      ]
    }
  ]
}
{% endschema %}
